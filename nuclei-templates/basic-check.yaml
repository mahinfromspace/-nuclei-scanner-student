id: basic-http-check

info:
  name: Basic HTTP Response Check
  author: student
  severity: info
  description: Checks if the website responds.

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers:
      - type: status
        status:
          - 200
          - 301
          - 302
          - 403
